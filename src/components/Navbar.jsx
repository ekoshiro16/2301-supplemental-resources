import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <header>
      <nav>
        <Link to="/">2301 javascript resources</Link>
      </nav>
    </header>
  );
};

export default Navbar;
